## Packages
framer-motion | Page transitions and scroll-triggered animations
embla-carousel-react | Gallery carousel for Pluga photos
lucide-react | Icons for the UI
react-hook-form | Form state management for orders
@hookform/resolvers | Zod validation for forms

## Notes
- Application relies heavily on RTL layout (Hebrew). Main wrapper must include dir="rtl".
- Payment is currently disabled per user request, but orders will still be saved to the database.
- Prices are mocked as they were not explicitly provided in the prompt.
- Admin dashboard is accessible at /admin to view orders.
